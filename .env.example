# Optional: override default config path used by `constellation`.
# Default: $XDG_CONFIG_HOME/constellation/config.toml
# CONSTELLATION_CONFIG=/etc/constellation/config.toml

# Optional: override default Rust log directives.
# Default: info,constellation=info
# RUST_LOG=info,constellation=debug
